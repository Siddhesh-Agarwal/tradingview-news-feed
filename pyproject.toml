[project]
name = "tradingview-news-feed"
version = "0.1.0"
description = "Python client for TradingView's news feed API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
]
